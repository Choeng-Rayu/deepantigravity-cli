{
  "name": "deepantigravity-proxy",
  "version": "0.1.0",
  "description": "HTTPS-MITM proxy that intercepts agy traffic to cloudcode-pa.googleapis.com and rewrites it to other LLM backends.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "node-forge": "^1.3.1"
  },
  "scripts": {
    "start": "node start-proxy.js",
    "regen-ca": "node cert.js"
  }
}
